# Elasticsearch配置
ES_HOST=localhost
ES_PORT=9200
ELASTICSEARCH_HOST=localhost
ELASTICSEARCH_PORT=9200
ELASTICSEARCH_TIMEOUT=30
ELASTICSEARCH_MAX_RETRIES=3
ELASTICSEARCH_INDEX_NAME=llm_index

# 阿里云API配置
# DashScope API配置
DASHSCOPE_API_KEY=your_dashscope_api_key

# 千问Turbo模型配置
QIANWEN_MODEL_NAME=qwen-turbo
QIANWEN_API_ENDPOINT=https://dashscope.aliyuncs.com/api/v1/services/aigc/text-generation/generation
QIANWEN_MAX_TOKENS=1500
QIANWEN_TEMPERATURE=0.7
QIANWEN_TOP_P=0.8

# 阿里云通用配置
ALIYUN_ACCESS_KEY_ID=your_access_key_id
ALIYUN_ACCESS_KEY_SECRET=your_access_key_secret
ALIYUN_REGION=cn-hangzhou

# 阿里云OSS配置
ALIYUN_OSS_BUCKET=your_bucket_name
ALIYUN_OSS_ENDPOINT=oss-cn-hangzhou.aliyuncs.com

# 阿里云PAI-EAS配置
ALIYUN_EAS_ENDPOINT=pai-eas.cn-hangzhou.aliyuncs.com
ALIYUN_EAS_SERVICE_NAME=your_service_name

# 嵌入模型配置
MODEL_NAME=text-embedding-v2
USE_GPU=false

# 应用配置
VECTOR_WEIGHT=0.5
TOP_K=3

# Web服务配置
HOST=127.0.0.1
PORT=8000

# 文本分割配置
CHUNK_SIZE=1000
CHUNK_OVERLAP=200